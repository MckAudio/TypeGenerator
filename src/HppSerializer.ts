import { ISerializer } from "./ISerializer";
import { ArrayType, ClassType, LinkType, SimpleType } from "./types";
import { GetDate } from "./tools";

export class HppSerializer extends ISerializer {
    protected begin() {
        this.source = `/**\n * @file ${this.namespaceName}.cpp\n`;
        this.source += ` * @author ${this.author}\n * @brief Autogenerated by MckAudio TypeGenerator\n`;
        this.source += ` * @date ${GetDate()}\n */\n\n`;
        this.source += `#pragma once\n\n`;
        this.source += `namespace ${this.namespaceName} {\n`;
    }

    end() {
        this.source += `} // namespace ${this.namespaceName}\n`;
    }

    addClassMember(member: ClassType) {

    }

    addSimpleMember(member: SimpleType) {

    }

    addLinkMember(member: LinkType) {

    }

    addArrayMember(member: ArrayType) {

    }

}